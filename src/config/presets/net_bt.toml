# NET Classic — who is here, the old way? B14's own question: classic
# Bluetooth has no small, fixed set of channels to gate on the way BLE's
# three advertising ones do, so this preset exists to be the thing a user
# picks to mean "point the radio at classic Bluetooth" - the same job
# `net_ble`'s own preset name already does for BLE.
#
# B15 gave this preset a live receiver: `net_bt_hops` is the exit condition
# made visible, a time-versus-channel scatter of access-code hits across
# however many of the 79 channels `[net].bt_channels` and the current view
# together let the worker watch. `net_bt_census` stays beside it, still
# saying plainly that a hit's LAP is not yet folded into a census entry -
# a different, narrower gap than B14's own "no receiver at all", and one
# B15 does not close.

section = "net"
slot    = 5
title   = "Classic"
blurb   = "who is here, the old way"

panels = [
    { name = "header",        position = "top", height = 5 },
    { name = "net_bt_hops",   position = "body" },
    { name = "net_bt_census", position = "right", width_pct = 34 },
    { name = "log",           position = "bottom", height = 5 },
    { name = "footer",        position = "bottom" },
]
