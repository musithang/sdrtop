# Micro sweep — [0] cycle step 5: field scanner glance (starts a sweep).

panels = [
    { name = "micro_sweep_panel",  position = "body" },
    { name = "footer",             position = "bottom" },
]
